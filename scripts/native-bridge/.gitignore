# Build artifacts - all outputs are regenerated via build.ps1 and are NOT tracked
# (the DLL was removed from source control 2026-07-08; rebuild with build.ps1,
# or package.ps1 rebuilds it during packaging).
*.dll
*.obj
*.exp
*.lib
*.pdb
*.ilk
